{
    "version": 2,
    "builds": [
      {
        "src": "build",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist",
          "precompress": false
        }
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/index.html" }
    ]
  }
  